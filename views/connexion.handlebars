
<div class="content container">
    <div class="row">
        <div class="col-md-12">
            <h2 class="text-center">Koneksyon </h2></div>
            <h2 class="text-center">{{login}}</h2>
    </div>
    <div class="row">
        <div class="col-md-6 col-md-offset-3 col-sm-6 col-sm-offset-3 col-xs-8 col-xs-offset-2">
            <form method="POST" action="/connexion">
                <div class="form-group">
                    <label class="control-label">Adrès mail</label>
                    <input name="email" class="form-control" type="email">
                </div>
                <div class="form-group">
                    <label class="control-label">Mo  pass</label>
                    <input name="password" class="form-control" type="password">
                </div>
                <input type="submit" class="btn btn-primary pull-right" value="Konnekte w'">
            </form><a href="/inscription">Mwen poko gen kont!</a></div>
    </div>
</div>

